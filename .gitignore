# ─── OS / Editor ────────────────────────────────────────────
.DS_Store
Thumbs.db
ehthumbs.db
*.swp
*.swo
*~
.atom/
.history
.build/
.buildlog/
.svn/
.swiftpm/
.fleet/
.vscode/
.idea/
*.iml
*.ipr
*.iws
migrate_working_dir/

# ─── AI tooling artifacts (NEVER commit) ───────────────────
.claude/
.opencode/
.aider*
.cursor/
.continue/
.cody/
.codex/
.puku
# ─── Flutter / Dart / Pub ───────────────────────────────────
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.flutter-plugins
.pub-cache/
.pub/
/build/
/coverage/

# ─── Symbolication / obfuscation ────────────────────────────
app.*.symbols
app.*.map.json

# ─── Android ────────────────────────────────────────────────
/android/app/debug
/android/app/profile
/android/app/release
/android/local.properties
/android/key.properties
/android/app/upload-keystore.jks
/android/.gradle/
/android/captures/
*.jks
*.keystore

# ─── iOS / macOS ────────────────────────────────────────────
**/ios/Pods/
**/ios/.symlinks/
**/ios/Flutter/Flutter.framework
**/ios/Flutter/Flutter.podspec
**/macos/Flutter/ephemeral/
**/macos/Pods/
.DS_Store
*.mode1v3
*.mode2v3
*.perspectivev3
xcuserdata/

# ─── Windows / Linux desktop ────────────────────────────────
**/windows/flutter/ephemeral/
**/linux/flutter/ephemeral/

# ─── Python (tools/ venv for build_kb.py) ───────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
tools/.venv/
tools/venv/
tools/env/
.env
.env.local
*.egg-info/
build/
dist/
.eggs/

# ─── AI model files (NEVER commit — downloaded separately) ─
*.task
*.litertlm
*.tflite
*.gguf
*.onnx
*.pb

# ─── Patch / merge artifacts ────────────────────────────────
*.orig
*.rej
*.patch
.git.rebase.head

# ─── Misc ───────────────────────────────────────────────────
*.class
*.log
# pubspec.lock IS committed for app projects (Flutter convention);
# `*.lock` is intended to keep Python package-lock.json / yarn.lock out.
package-lock.json
yarn.lock
crashlytics/
# Service-account private keys — real secrets. NOT `firebase*.json`, which
# also swallowed firebase.json / firestore.indexes.json: those are deploy
# config with nothing sensitive in them and belong in the repo, so
# `firebase deploy --only firestore:rules` works on a fresh clone.
firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
serviceAccountKey.json
# Per-developer download from the Firebase console. Contains the project's
# public Android API key — not a secret, but it is per-project config that
# each developer fetches themselves.
google-services.json
GoogleService-Info.plist
*.tmp
*.bak
# Release APKs — published via GitHub Releases, not committed to git
Shongjog-*.apk
*.apk
!pubspec.lock

# ─── Team-local AI tooling (per-developer, not project code) ─
.agents/
.vercel
.env*

# ─── Course study site (teaching material, hosted on Netlify) ──
/site/
/PRODUCT.md

/.impeccable/
/shongjog-site-v2.zip

# Eval harness generated artifacts (Colab T4 outputs)
/eval/results/

# EmbeddingGemma bundled payloads (regenerated by tool/bundle_embedder.py
# before a prebuilt APK build; too large to commit at ~175 MB). The
# directory itself must exist for pubspec's asset list — only .gitkeep is
# committed; see the exception below.
/assets/embeddinggemma/*
!/assets/embeddinggemma/.gitkeep

# Internal advisor / skill audit plans — local-only design notes, not
# repo source. Reviewer-facing design lives in docs/ instead.
/advisor-plans/
